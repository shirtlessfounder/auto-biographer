# social-posting

Small TypeScript service foundation for the semiautonomous X posting workflow.

## Local setup

```bash
cd /Users/dylanvu/social-posting
pnpm install
```

Copy `.env.example` into your local environment before running commands.

## Commands

```bash
pnpm test
pnpm typecheck
pnpm cli -- migrate
pnpm cli -- check-env
```
